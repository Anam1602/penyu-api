{
  "version": 2,
  "builds": [{ "src": "api/**/*.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/api/(.*)\\.js", "dest": "/api/$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "crons": [
    { "path": "/api/aggregate-5min", "schedule": "0 0 * * *" }
  ]
}
